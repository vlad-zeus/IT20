let a = prompt("Введите число", "0");
let length_days = a.length;
let last_symbol = a.slice(-1);
alert(length_days + last_symbol);
if (length_days === 1) {
  if (last_symbol === 1 || (last_symbol >= 5 && last_symbol <= 9))
}
